"use client";
interface CounterProps {
  title: string;
  subtitle: string;
  value: number;
  onChange: (value: string) => void;
}
const Counter = () => {
  return <div></div>;
};

export default Counter;
